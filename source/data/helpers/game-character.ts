import { GameCharacter, User } from "@roll4init/objects";

export class GameCharacterNodeHelper {
    public static async fetchByUnique(): Promise<GameCharacter> {
        return null;
    }

    public static async fetchByOwner(): Promise<GameCharacter[]> {
        return [];
    }
}

export default GameCharacter;
